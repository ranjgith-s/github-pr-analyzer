# Environment variables for github-pr-analyzer (Vite)
# Copy this file to `.env.local` (recommended) or `.env` and fill in the values.
# For Vite, variables must be prefixed with VITE_.

# Supabase project URL (from Supabase: Project Settings → API → Project URL)
VITE_SUPABASE_URL='https://your-supabase-url.supabase.co'

# Supabase anonymous public API key (from Supabase: Project Settings → API → Project API keys → anon key)
VITE_SUPABASE_ANON_KEY='your-anon-key'
